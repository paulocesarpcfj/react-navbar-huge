.navbar
    position fixed
    width 100%
    height 6rem
    display flex
    background $mainColor
    color white
    align-items center
    z-index 10

    @media screen and (max-width 768px)
        justify-content space-between
